# Copy to .env (gitignored) and fill in. Simyoke loads .env at startup.
# Only the AI paths need this: `simyoke record` and `simyoke run --dismiss-alerts`.
# Real environment variables take precedence over this file.
ANTHROPIC_API_KEY=sk-ant-...
