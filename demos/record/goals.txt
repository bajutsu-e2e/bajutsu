# Example goals for demos/record/generate_from_nl.py — one goal per line.
# Blank lines and lines starting with `#` are ignored.
#
# Run all of them:  uv run python demos/record/generate_from_nl.py --file demos/record/goals.txt
# Run just one:     uv run python demos/record/generate_from_nl.py "<paste a line below>"
#
# These match the demo's mock app, which mirrors the bundled `sample` app
# (onboarding -> login -> home, with a counter on Home). The `wait for Home` clause
# lets the generated scenario survive the login screen transition on a real device,
# the same way sample/scenarios/smoke.yaml does. See this folder's README for the
# small grammar the stand-in agent understands.

# Log in, wait for Home, increment the counter twice, then assert its value.
Get started, log in with email demo@bajutsu.dev and password hunter2, wait for Home, then tap Increment twice, and check the counter shows 2

# A single increment (note "shows 1").
Get started, log in with email a@b.co and password pw, wait for Home, then tap Increment, and check the counter shows 1

# The "N times" count and the "press" verb, asserted with "verify".
Get started, log in with email user@example.com and password s3cr3t, wait for Home, then press Increment 3 times, and verify the counter shows 3

# The "thrice" count and the "click" verb, asserted with "confirm".
Get started, log in with email qa@team.dev and password p4ss, wait for Home, then click Increment thrice, and confirm the counter shows 3

# Actions only — no assertion; the scenario just performs the steps.
Get started, log in with email me@demo.app and password pw123, wait for Home, then tap Increment twice

# Minimal: just the login flow, no counter.
Get started, log in with email someone@host.io and password letmein
