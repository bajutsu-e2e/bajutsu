# Alembic config for the hosted backend's system of record (BE-0015 7a-2).
# The database URL comes from the BAJUTSU_DATABASE_URL environment variable (see migrations/env.py),
# so it is intentionally left blank here. From this directory: `alembic upgrade head`.
[alembic]
script_location = %(here)s/migrations
