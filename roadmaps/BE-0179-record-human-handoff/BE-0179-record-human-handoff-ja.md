[English](BE-0179-record-human-handoff.md) · **日本語**

# BE-0179 — record 中の human-in-the-loop ハンドオフ（一時停止・引き渡し・再開）

<!-- BE-METADATA -->
| 項目 | 値 |
|---|---|
| 提案 | [BE-0179](BE-0179-record-human-handoff-ja.md) |
| 提案者 | [@0x0c](https://github.com/0x0c) |
| 状態 | **実装済み** |
| トラッキング Issue | [検索](https://github.com/bajutsu-e2e/bajutsu/issues?q=is%3Aissue+label%3Aroadmap-tracking+in%3Atitle+"BE-0179") |
| 実装 PR | [#735](https://github.com/bajutsu-e2e/bajutsu/pull/735) |
| トピック | オーサリング体験 |
| 関連 | [BE-0011](../BE-0011-local-web-ui-serve/BE-0011-local-web-ui-serve-ja.md), [BE-0012](../BE-0012-action-capture-record/BE-0012-action-capture-record-ja.md), [BE-0014](../BE-0014-record-demarcation/BE-0014-record-demarcation-ja.md), [BE-0039](../BE-0039-self-healing-propose-optin/BE-0039-self-healing-propose-optin-ja.md), [BE-0044](../BE-0044-scenario-provenance/BE-0044-scenario-provenance-ja.md), [BE-0046](../BE-0046-otp-email-steps/BE-0046-otp-email-steps-ja.md), [BE-0098](../BE-0098-unified-authoring-surface/BE-0098-unified-authoring-surface-ja.md), [BE-0120](../BE-0120-recorded-scenario-secret-tokenization/BE-0120-recorded-scenario-secret-tokenization-ja.md) |
<!-- /BE-METADATA -->

## はじめに

record における human-in-the-loop の二つのパターン、すなわち人による値入力
（`record-human-value-prompt`）と人による操作の引き取り（`record-human-takeover-step`）が
共有する土台です。本項目は、AI 駆動の `record` ループが途中で一時停止し、人に制御を引き渡し、
その入力や操作を受け取ってから再開する仕組みを定めます。具体的には、一時停止の契機、要求と応答の
契約、CLI と `serve` の両インターフェース、そしてそれらすべてを縛る不変条件です。不変条件とは、
ハンドオフが記録する成果物は、実行経路に人を置かずに再び決定論的に実行できなければならない、という
ものです。二つのパターンはこの土台の上に乗り、記録される成果物の「形」を決めます。本項目が担うのは
「仕組み」と「境界」です。

## 動機

`record` は AI エージェントを 1 ステップずつ駆動します。画面を観測し、次の操作を提案し、それを
実行する、という流れです（[`bajutsu/record.py`](../../bajutsu/record.py) の `record()`
ループ）。エージェントに有効な手が無いとき、`record` は単に停止します。エージェントが操作を提案
しないか、提案されたターゲットが実際の画面で解決できないか（`could not resolve that target on the
live screen; stopping`）のいずれかです。人に助けを求める経路はありません。

このため、AI が供給できない何かで塞がれた flow は、端から端まで記録できません。エージェントが
入力欄は見えているのに値を知り得ないワンタイムパスワード（OTP）や二要素認証（2FA）のコード、
エージェントが解けない CAPTCHA や生体認証のプロンプト、繰り返し解決に失敗するジェスチャやターゲット
などです。現状では、著者はその手前で記録を諦め、残りのシナリオを手で書くしかありません。これは
`record` が取り除くために存在する、まさにその手作業です。

塞がれた箇所ごとに個別の抜け道を継ぎ足すのではなく、ツールには一つのプリミティブが必要です。
ループを一時停止し、人に明確な要求を提示し、応答を受け取り、実際の画面から再開する、という
プリミティブです。二つの具体的なケース、すなわち値を供給する場合と操作を行う場合は、何を求め何を
記録するかが違うだけで、ループがどう引き渡すかは変わりません。

この仕組みは prime directive の内側に収まります。全体が **Tier 1**（`record` とオーサリング）に
とどまるからです。人はオーサリングの最中にループへ入るのであって、決定論的な `run` や CI のゲート
には決して入りません。土台の中心的な責務は、この境界を守ることにあります。あらゆるハンドオフは
再実行可能な成果物へ解決されなければならず、そうであれば人の助けを借りて作った記録も、人が居ない
まま再生できます。ハンドオフが人だけに依存する要素を実行経路に焼き込めてしまう設計は、directive 1
と 2 に反します。それを防ぐことが本項目の役割です。

## 詳細設計

作業の対象は仕組みとその保証です。パターンごとの挙動は二つの子項目が受け持ちます。

**一時停止の契機。** ハンドオフは、静かな停止ではなく明示的な要求として、次の二つの状況で立ち上がり
ます。(a) エージェント自身が先へ進めないと表明したとき（既存の `done` や「操作なし」とは区別される、
新しい「人が必要」というターン結果）、(b) 著者が任意のターンで自ら引き取りを要求したとき、です。
特定の詰まりに対する検出のヒューリスティック（OTP らしき入力欄、解決できないターゲット）は子項目に
属します。本項目が定めるのは、その結果と、ループがそれにどう反応するかだけです。

**要求と応答の契約。** 転送方式に依存しないハンドオフの「要求」（何を、なぜ求めるか、そして現在の
画面。要素の要約とスクリーンショット）と、「応答」（供給された値、または「デバイスを操作したので
観測し直せ」）を定義します。一度だけ定めることで、CLI と `serve` の両インターフェースが同じ
プロトコルを実装し、二つの子パターンがそれをそのまま再利用できます。

**CLI インターフェース。** 端末から駆動する `record` は、要求で待機し、対話的なプロンプト（標準
入力）から応答を読み取ります。待機は上限つきで中断可能とし、際限なくハングすることはありません。
応答者の居ない非対話的な実行や CI での実行は、永久に待つのではなく、後述のとおり明確に失敗します。

**`serve` インターフェース。** ハンドオフにとって、より自然な住処は Web UI です。`serve` から
`record` を駆動しているとき、人はすでにブラウザでその進行を見ています。ですから助けを求めて一時
停止することは、別のウィンドウへ切り替える動作ではなく、文脈の中の小さな一歩になります。`serve` の
record 経路には、設計を左右する三つの性質があります。

- **プロセス境界を跨ぐ。** `serve` は `bajutsu record` をバックグラウンドジョブとして spawn し
  （[`bajutsu/serve/operations/dispatch.py`](../../bajutsu/serve/operations/dispatch.py)、
  BE-0127）、その進捗を server-sent events で配信します（record の実況は既にこの経路でブラウザへ
  流れています）。したがってハンドオフの要求は、プロセス内のコールバックではなく**シリアライズ
  可能**でなければなりません。要求は同じストリーム上の構造化イベントとしてブラウザへ出て行き、人の
  応答は応答用のエンドポイントを通って戻ってきます。前述の転送方式に依存しない契約が、これを CLI が
  プロセス内で使うのと同じプロトコルにします。
- **ジョブが明示的な「人待ち」状態に入る。** ワーカーが見えないまま待つのではなく、一時停止した
  record は UI の中で目に見える、再開可能なジョブ状態になります。record ビュー（統合オーサリング
  サーフェス、BE-0098）は要求をモーダルで描画します。折りたたみの下に埋もれて見落とすことがないよう
  にするためです。なぜループが止まったか、現在の画面（要求が運ぶスクリーンショット。既存のキャプチャ用
  スクリーンショット経路を再利用します。対象要素は文字列の説明として運び、ピクセル単位の強調は
  follow-up です）、そして応答用のコントロールです。人はブラウザで答え、ループが再開します。
- **上限つきで中断可能。** モーダルには中断（record をきれいに終える）を用意し、人待ち状態は CLI と
  同じ上限つきの待機を持ちます。ですから `serve` のワーカーが、席を外した人を相手に際限なくハング
  することはありません。

二つの子パターンは、この一つの UI 要素の中で描画されます。値パターンなら値の入力欄、引き取り
パターンなら「デバイスを操作した。再開する」というコントロールです。

**再開の意味論。** 応答のあと、ループは実際の画面を問い合わせ直し、観測した状態から続行します。
観測して提案して実行する、という同じサイクルです。人の寄与（入力した値、手で行った操作）は新しい
起点として吸収されるので、下流で特別扱いする必要はありません。

**決定論的な出力という不変条件。** 土台は、ハンドオフが再実行可能な成果物へ解決されるという契約を
定義し、それを強制します。成果物の形（値のプレースホルダか、明示的な手動ステップか）を土台自身が
選ぶことはしません。それは子項目の判断です。土台が保証するのは、いかなるハンドオフも、人だけに
依存する要素を実行経路へ静かに記録できないことです。

**非対話的な実行と CI での挙動。** 人の応答者が居ない環境（CI、ヘッドレス）で `record` を走らせた
とき、立ち上がったハンドオフは明確でラベル付きの失敗になります（「この flow は人によるハンドオフが
必要です。対話的に記録し直してください」）。際限のないハングにも、AI による推測にもなりません。
これにより、自動化のもとでツール自体の決定性が保たれます。

## 検討した代替案

- **塞がれた箇所ごとに個別のコード経路で解決し、共有の土台を持たない。** 却下します。値のケースと操作
  のケースがそれぞれ一時停止と再開の配管を作り直し、しかも両者が CLI と `serve` の双方でそれを重複
  させます。仕組みを一度だけ切り出すことが、二つの子項目を小さく一貫させます。
- **エージェントに値や操作を推測（ないし hallucinate）させて先へ進める。** 却下します。これは
  determinism first に反します。誤った値や誤ったタップは記録を静かに壊しますし、それこそ本作業が
  取り除く失敗の形です。AI が解決できない詰まりは、覆い隠すのではなく表に出さなければなりません。
- **実行経路に「人を待つ」ステップを置く（毎回の `run` が止まる手動ステップ）。** 土台のレベルで
  却下します。それは決定論的な `run` と CI のゲートに人を置くことであり、directive 1 と 2 に反します。
  土台は、人を record の時点にとどめ、決定論的な出力を強制するためにこそ存在します（手動の目印が避け
  られない、明示的に CI 非対象の狭いケースは操作引き取りの子項目で論じますが、そこでも CI で静かに
  合格することは決してありません）。

## 進捗

> 開発の進行に合わせて常に最新の状態に保ってください。チェックリストは *詳細設計* の MECE な
> 作業分解（作業の単位ごとに 1 つ）に対応し、ログには変更内容と時期（古い順）を PR へのリンクと
> ともに記録します。

- [x] 「人が必要」というターン結果を、`done` や操作なしとは別に、エージェントと record ループへ追加する。
- [x] 転送方式に依存しないハンドオフの要求と応答の契約を定義する。
- [x] CLI インターフェース: 標準入力での、上限つきで中断可能な対話プロンプト。
- [x] `serve` インターフェース: 要求を record の SSE ストリームでシリアライズ配信し、応答をエンドポイントで受け取る（spawn した `bajutsu record` のプロセス境界を跨ぐ、BE-0127）。*ローカルの `serve` のみです。分散サーバーバックエンド（リモートワーカー、BE-0015）の応答チャネルは follow-up とします。*
- [x] `serve` インターフェース: 明示的で再開可能な「人待ち」ジョブ状態を、record ビュー（BE-0098）のモーダルとして、要求のスクリーンショットと応答コントロールとともに描画する。
- [x] 観測し直して再開する配線を record ループへ組み込む。
- [x] 非対話的な実行と CI での挙動: ハングも推測もない、明確でラベル付きの失敗。

この土台の最初の一歩からは、次の三つを follow-up に回します。一つ目は、著者が自ら一時停止を要求する、明示的な引き取りの契機（(b)）です。これは、ここで実装したエージェント発の「人が必要」の契機（(a)）とは別のものです。二つ目は、分散サーバーバックエンドの応答チャネルです。三つ目は、要求のスクリーンショット上で対象要素をピクセル単位で強調する機能で、対象は今のところ文字列の説明として運びます。二つの子パターン（`record-human-value-prompt` と `record-human-takeover-step`）が、「人が必要」の結果を立ち上げるヒューリスティックと、記録される成果物の形を受け持ちます。

**ログ**

- 土台を実装しました。ハンドオフの契約（[`bajutsu/handoff.py`](../../bajutsu/handoff.py)）、record ループの「人が必要」というターン結果と一時停止・再開（[`bajutsu/record.py`](../../bajutsu/record.py)）、CLI の標準入力応答（[`bajutsu/cli/handoff.py`](../../bajutsu/cli/handoff.py)）、そしてローカル `serve` のインターフェース（SSE の `human-request` イベント、record プロセスの標準入力を通す `respond-human` エンドポイント、「人待ち」ジョブ状態、Web UI のハンドオフモーダル）です。非対話的な実行と CI では、明確でラベル付きの失敗を立ち上げます。
- デモを用意しました。web デモアプリのデバイス検証フロー（[`demos/web/app/index.html`](../../demos/web/app/index.html)）と、オーサリングエージェント（[`bajutsu/claude_agent.py`](../../bajutsu/claude_agent.py)）が知り得ない値（out-of-band のワンタイムコード）に対して出す `ask_human` の結果です。これにより、実際の headed `record` が端から端までハンドオフします（`make -C demos/web record-handoff`）。ツールチェーン向けにキー不要のオフライン版も用意しました。この `ask_human` の指示は最初の最小の一歩で、欄ごとのヒューリスティックと記録される成果物の形は value-prompt / takeover の子項目が受け持ちます。

## 参考

この土台の上に乗る子パターン: `record-human-value-prompt`（値）と `record-human-takeover-step`
（操作）。関連する既存項目:
[BE-0046 — OTP・メールの側方チャネルステップ](../BE-0046-otp-email-steps/BE-0046-otp-email-steps-ja.md)
（値に対する、実行時の決定論的な橋渡し先）、
[BE-0012 — 操作キャプチャ record](../BE-0012-action-capture-record/BE-0012-action-capture-record-ja.md)
（AI を使わない、人の実演による記録）、
[BE-0039 — 自己修復は「提案＋opt-in 適用」に限定](../BE-0039-self-healing-propose-optin/BE-0039-self-healing-propose-optin-ja.md)
（提案してから人が承認するパターン）、
[BE-0014 — 既存 AI record との棲み分け](../BE-0014-record-demarcation/BE-0014-record-demarcation-ja.md)、
[BE-0098 — serve の統合オーサリングサーフェス](../BE-0098-unified-authoring-surface/BE-0098-unified-authoring-surface-ja.md)、
[BE-0120 — 記録された scenario の YAML でシークレットをトークン化する](../BE-0120-recorded-scenario-secret-tokenization/BE-0120-recorded-scenario-secret-tokenization-ja.md)。
[`bajutsu/record.py`](../../bajutsu/record.py)、[`bajutsu/agent_protocols.py`](../../bajutsu/agent_protocols.py)。
