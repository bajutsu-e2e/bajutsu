[English](BE-0418-ios-alert-guard-one-shot-retry.md) · **日本語**

# BE-0418 — ステップ終了時のアラートガードで複数件のアラートを解消できるようにし、外れたタップを取りこぼさないようにする

<!-- BE-METADATA -->
| 項目 | 値 |
|---|---|
| 提案 | [BE-0418](BE-0418-ios-alert-guard-one-shot-retry-ja.md) |
| 提案者 | [@0x0c](https://github.com/0x0c) |
| 状態 | **実装済み** |
| トラッキング Issue | [検索](https://github.com/bajutsu-e2e/bajutsu/issues?q=is%3Aissue+label%3Aroadmap-tracking+in%3Atitle+"BE-0418") |
| 実装 PR | [#1979](https://github.com/bajutsu-e2e/bajutsu/pull/1979) |
| トピック | Platform support |
| 関連 | [BE-0269](../BE-0269-ios-alert-guard-early-wait-intervention/BE-0269-ios-alert-guard-early-wait-intervention-ja.md), [BE-0315](../BE-0315-ios-native-system-alert-handling/BE-0315-ios-native-system-alert-handling-ja.md), [BE-0399](../BE-0399-ios-system-alert-interruption-policy/BE-0399-ios-system-alert-interruption-policy-ja.md), [BE-0402](../BE-0402-run-alert-guard-drop-vision-fallback/BE-0402-run-alert-guard-drop-vision-fallback-ja.md), [BE-0406](../BE-0406-system-alert-declared-prompts/BE-0406-system-alert-declared-prompts-ja.md) |
<!-- /BE-METADATA -->

## はじめに

`systemAlertHandling` のガードは、画面をブロックしているシステムアラートに 2 つの異なる仕組みで答えて
います。どちらが動くかは、シナリオのどこでアラートに出会うかで決まります。`wait` ステップは
`_AlertGuardGate` を駆動します。このガードは毎回のポーリングで画面を読み、`wait` が許す限りのポーリング
回数にわたって、複数件のアラートを順に解消できます。一方、`tap` のような通常のアクションステップには、
そうしたポーリングのループがありません。ステップが失敗した時点で、`AlertGuardConfig.__call__` が画面を
ブロックしているものを解消する機会は 1 回だけ与えられます。その後ステップが 1 回だけ再試行し、実行は
先へ進みます。

本提案は、この 1 回きりの試行を、複数件のアラートを解消でき、かつ 1 回目のタップが外れても取りこぼさない
ものにします。現状はどちらもできていません。1 回の呼び出しで解消できるアラートは最大 1 件です。しかも、
アプリ自身のプロセス内に出るアラートを解消できる唯一の経路では、タップが `ElementNotTappable` を返した
瞬間に諦めます。この「まだ到達できない」という状態には、`_AlertGuardGate` のほうは実機での計測結果を
踏まえて、上限付きのリトライを備えています。その結果、2 件のプロンプトが近接して現れたときや、
タップがまだアニメーション中のアラートを捉えてしまったときに、ステップはアラートに関する注記を一切
残さないまま失敗します。どちらも iOS ではありふれた状況であり、ポーリング側のガードはすでにどちらにも
対応しています。

## 動機

シナリオの `tap` ステップが、アプリ自身のプロセス内で出るアラート（特に iOS の「パスワードを保存」
プロンプト）に対して、断続的に失敗することがあります。失敗理由にはアラートについての情報が一切なく、
ステップが到達できなかった対象要素の名前だけが残ります。同じシナリオをもう一度実行すると成功します。
このような 1 回の実行を調査したところ、`AlertGuardConfig`
([bajutsu/common/orchestrator/types/alert_guard_config.py](../../bajutsu/common/orchestrator/types/alert_guard_config.py))
（反応的なガードのシナリオ単位の設定を持つクラスです）のなかに、具体的な原因が 2 つ見つかりました。
どちらも同じ設計上の選択、つまり `__call__` メソッドがループではなく単発の解消試行である点に由来して
います。

1 つめの原因は、複数件が重なった場合の抜けです。iOS は、1 回のユーザー操作のあとに複数のプロンプトを
連続して出すことがよくあります。たとえば、同じサインイン操作から、通知許可の要求が、パスワード保存
プロンプトの前後どちらかに連なって出ることがあります。`__call__`（172〜191 行目）は `probe_native`
を読みます。そのプローブが `"dismissed"` を返した瞬間に処理を終えます。SpringBoard 側の
プロンプトは解消されますが、アプリ自身のプロセスにパスワード保存シートがまだ残っているかどうかは、
一度も確認されません。呼び出し元は「アラートを 1 件解消した」と見なしてステップを 1 回だけ再試行します。
その再試行は、`__call__` が一度も見なかったシートに阻まれて、理由の説明もないまま再び失敗します。

2 つめの原因は、タップの着地をめぐる競合で、こちらは単一のアラートでも起こります。`dismiss_from_tree_once`
は、`wait` 中に動く `_AlertGuardGate._dismiss_from_tree` の単発版にあたり、アラートのボタンをタップし
ます。この関数は `ElementNotTappable` を「引き上げるべき理由」として扱い、`None` を返して注記も残さず、
再試行もしません。ところが `wait` 中のガードは、同じ例外を別のものとして扱っています。そちらの
docstring には、シートの登場アニメーションが終わるまでボタンをスクリムが覆ったままにする挙動が
記録されています。そのため
[`_alert_guard_gate.py`](../../bajutsu/common/orchestrator/waits/_alert_guard_gate.py) の
329〜343 行目は、`_TREE_RETAP_DELAY` と `_TREE_DISMISS_MAX_TAPS` による上限付きのリトライを
備えています。別のアラートの陰から出てきたばかりのアラート、つまり重なったプロンプトを 1 件ずつ
解消していく場面は、まさにこの窓のただなかにあります。単発の経路が唯一の試行機会を得るその瞬間、
当のアラート自身の登場アニメーションはまだ終わっていないことが少なくありません。

2 つの原因は 1 つの根に帰着し、1 つの直し方で答えられます。`__call__` は、画面が実際に晴れるまで、
あるいは小さな上限に達するまでループするのではなく、何かがブロックしているかどうかを 1 回だけ判定して
止まります。`_AlertGuardGate` がすでにループしているのは、`wait` ステップ自身のポーリング周期が
1 ポーリングごとに 1 回、このガードを駆動しているからです。`AlertGuardConfig.__call__` にはそうした
周期が与えられていないため、みずからループする必要があります。この提案が実を結んだかどうかは、次の
状況を再現すればわかります。通常のアクションステップのあと、割り込みをカバーする `wait` ステップを
置かないまま、2 件のアラートが近接して現れる場合です。そのシナリオは、以後は確実に成功するはずです。
それでも失敗するステップは、ガードが解消できなかったアラートを自分の理由のなかで名指しするはずです。
画面上のプロンプトについて何も語らない、ただの「要素が見つからない」で終わってはいけません。

## 詳細設計

### ユニット 1 — 画面が晴れるか上限に達するまで `AlertGuardConfig.__call__` をループさせる

`__call__` を、単発の `probe_native` / `dismiss_from_tree_once` の組から、同じ組を繰り返す上限付きの
ループへ変更します。新しい定数 `_GUARD_CALL_MAX_ROUNDS`（提案値: 3）がこのループを打ち切ります。
この値は、`wait` 中のガード自身のタップ上限である `_TREE_DISMISS_MAX_TAPS` と同じ桁数に合わせています。
各ラウンドは、次の 4 つのうちどれか 1 つを行います。

1. `probe_native` を呼びます。`"dismissed"` という結果が返れば、その場でイベントを記録して次のラウンド
   へ進みます。すぐに戻り値を返すことはしません。この変更により、SpringBoard 側のアラートの陰に
   ツリー内アラートが隠れている場合でも、2 件目のアラートを読む前に呼び出しが終わってしまうことは
   なくなります。
2. `"absent"` であれば、ユニット 2 で改める `dismiss_from_tree_once` を呼びます。`AlertEvent` が返れば、
   ネイティブな解消と同様に、イベントを記録して次のラウンドへ進みます。
3. 同じく `"absent"` の場合で、`dismiss_from_tree_once`（ユニット 2）が `"not_tappable"` を返したときは、
   アラートを解消したことにはせず、次のラウンドへ進みます。`wait` 中のガードがアニメーション中のスクリム
   に与えているのと同じ猶予です。
4. それ以外の結果、つまり `"unhandled"` ・ `"reserved"` ・ `"incapable"`、あるいは `dismiss_from_tree_once`
   がもう何にも一致しない `"absent"` であれば、ループを終えます。この時点で残っているものに対して、
   ここから先にできることは現状と変わらず何もありません。

いずれかのラウンドがアラートを解消したか、あるいは `"not_tappable"` を返した場合は必ず、次のラウンドが
画面を読む前、あるいは呼び出しが戻る前に、既存の `settle_after_alert_dismiss` を実行します
（[`_functions.py:687`](../../bajutsu/common/orchestrator/waits/_functions.py)）。
`_GUARD_CALL_MAX_ROUNDS` を使い切るラウンドもこれに含みます。そのラウンドには、落ち着かせるべき
次のラウンドがもう無いからです。
ユニット 3 は、この保証がどの終わり方でも成り立つことを根拠に、呼び出し元自身の落ち着かせ処理を削ります。
これは、現状すでに 1 回だけ実行されている動作と変わりません。タップした直後のプロンプトが、その時点で
もう消えているとは限りません。ツリーがまだアニメーション中のまま読んでしまうラウンドは、何にも一致
しないおそれがあります。

`blocked_note` は既存の意味を保ちます。つまり、プローブが見て、解消できなかったものです。その意味が
今日まだ覆えていなかった 1 つのケースを、この提案は補います。ループが `_GUARD_CALL_MAX_ROUNDS` を
`"not_tappable"` で使い切った場合、`blocked_note` に `uncleared_prompt_note(label)` をセットします
（[`types/_functions.py:51`](../../bajutsu/common/orchestrator/types/_functions.py)）。これは、
ユニット 2 の戻り値が運ぶラベルを名指しするもので、`_alert_guard_gate.py` が 1 回分の表示を諦める
ときに書くのと同じ注記です。これがないと、ネイティブなプローブは SpringBoard クエリから見えない
アプリ内プロセスのシートを `"absent"` と報告し、既存の `state == "unhandled"` という条件は注記を
空のままにします。すると、本提案がまさに解決しようとしている動機の場面が、それでもただの要素欠落
として読めてしまいます。ループが上限に達してもステップが失敗する場合、その理由には残っていたアラートの
名前が入るようになります。ネイティブ側の未対応アラートか、ツリー内で塞がれたアラートか、どちらの出所
であってもです。

### ユニット 2 — ツリー内タップでも、`wait` 中のガードと同じ着地レースを取りこぼさないようにする

`dismiss_from_tree_once` は、`ElementNotTappable` を `ElementNotFound` ・ `AmbiguousSelector` と
同じ節でまとめて扱うのをやめます。現状は 1 つの `except` 節が 3 つとも `None` で答えています
（[`alert_guard_config.py:165`](../../bajutsu/common/orchestrator/types/alert_guard_config.py)）。
`ElementNotTappable` は独自の分岐を持ちます。戻り値は `AlertEvent | None` から、3 つの結果を区別できる
形へ広がります。解消できた場合はこれまでどおり `AlertEvent` です。見えてはいるがまだ到達できない場合は、
`"not_tappable"` という印に加えて、タップを試す前にこの関数がすでに `match_alert_rule` で解決していた
ラベルを添えます。新たな照会は要りません。何もすることがない場合は `None` のままです。プロンプトが
自ら閉じたか、一致があいまいになったかのどちらかです。残り 2 つの例外はこれまでどおり `None` を返して
ループを終わらせます。自ら閉じたプロンプトや、あいまいに解決したラベルを再試行しても、
`_GUARD_CALL_MAX_ROUNDS` の予算を、決して着地しないタップに使い切るだけだからです。

これは `_alert_guard_gate.py` が同じ例外に対してすでに行っているリトライをなぞるものであり、その完全な状態機械
（`_TREE_RETAP_DELAY`・`_TREE_DISMISS_MAX_TAPS`・`_tree_signature`）をそのまま複製するわけではありません。
`wait` 中のガードがその仕組みを必要とするのは、`wait` ステップのポーリング周期が同じ 1 件のアラートに
対してこの関数を何度も呼びうるからです。そのたびに、ゆっくり消えていくフェードなのか、それともアプリ
が一度も反応しなかったタップなのかを見分けなければなりません。`dismiss_from_tree_once` は失敗した
ステップにつき 1 回しか呼ばれないので、そこでは状態を呼び出しをまたいで持ち越さずに、短くラウンド数で
上限を切ったリトライだけで同じ目的を果たせます。

### ユニット 3 — `__call__` の契約を、解消したアラートをすべて報告する形に変える

`__call__` のシグネチャを、`(driver: base.Driver) -> AlertEvent | None` から
`(driver: base.Driver, alerts: list[AlertEvent]) -> bool` へ変更します。これは、1 件のイベントを返す
のではなく呼び出し元が渡したリストへ直接追記する、`_AlertGuardGate` がすでに使っているやり方に合わせた
ものです。単一の `AlertEvent | None` という戻り値では、複数ラウンドにわたる呼び出しが解消した内容を
表現しきれません。最後のイベントだけを読む呼び出し元は、それより前のラウンドが解消したアラートを
すべて報告し損ねてしまいます。
[BE-0406](../BE-0406-system-alert-declared-prompts/BE-0406-system-alert-declared-prompts-ja.md) の
ユニット 2a も、別の呼び出し箇所について同じ過少報告のおそれを指摘していました。

呼び出し箇所は両方とも、この新しい契約へ移ります。それに合わせて、再試行の判定だけでなく注記を
付け足す条件も変わります。現状、`event is not None` であることと `blocked_note` が空でないことは
両立しません。`__call__` は `"dismissed"` のときに注記を消すからです。そのため
`if event is None and note …` は、今のところこの判定だけで両方の呼び出し元を満たせています。複数
ラウンドにわたる呼び出しは、この両立しない関係を崩します。ラウンド 1 が SpringBoard 側のプロンプトを
解消し、ラウンド 2 が 2 件目を `"unhandled"` のまま残すことがあるからです。この場合 `cleared` は
`True` でありながら `blocked_note` は空ではありません。両方の呼び出し箇所は、これ以降、注記の有無
だけで判定します。`cleared` では判定しません。こうすれば、重なったケースで呼び出しが何かを解消して
いたとしても、残っていたものをきちんと名指しできます。再試行するかどうかは `cleared` だけで決めます。

- [`_step_runner.py:653–695`](../../bajutsu/common/orchestrator/loop/_step_runner.py) — ステップ終了時
  の再試行です。`event = self.cfg.alert_guard(active_driver)` は、`outcome.alerts` に直接追記する
  呼び出しへ変わります。続く `if event is not None` の分岐は `if cleared` になります。隣にある注記の
  分岐は `if not cleared` にはなりません。`if event is None and note` は、上の段落のとおり、注記の
  有無だけを見る `if note and note not in reason` になります。`settle_after_alert_dismiss` はここでは
  呼ばれなくなります。ユニット 1 が、解消した、または着地待ちだったラウンドの後に、最後のラウンドを
  含めてすでに落ち着かせているからです。
- [`_functions.py:752–769`](../../bajutsu/common/orchestrator/loop/_functions.py) — `expect` の再試行
  です。ここでガード自身が返す結果は `expect_alerts.append(event)`
  （[`loop/_functions.py:760`](../../bajutsu/common/orchestrator/loop/_functions.py)）であり、これが
  直接の追記に変わります。750 行目の `expect_alerts.extend(...)` は `drain_interruptions` の結果で、
  変更しません。`if event is None and alert_guard.blocked_note` も同様に、
  `if note and note not in reason` へ変わります。

どちらの呼び出し箇所も、それ自身の再試行回数は変わりません。ステップや `expect` は、ガードが動いた
あとに今日と同じく 1 回だけ再試行します。変わるのは、その 1 回の呼び出しのなかでガード自身がどこまで
解消できるかという点だけです。

### ユニット 4 — テスト

[`tests/orchestrator/test_native_alert_guard.py`](../../tests/orchestrator/test_native_alert_guard.py)
の既存の単発経路のテストに、`FakeDriver` を使った新しいテストを加えます。

- 複数件が重なるケース: ネイティブ経路でしか解消できないプロンプトの陰に、ツリー内でしか解消できない
  プロンプトが隠れています。1 回の `__call__` から、両方のイベントが呼び出し元の `alerts` リストへ
  入ることと、その後のステップの再試行が晴れた画面に対して成功することを確認します。
- 2 件目が未対応のまま残るケース: ネイティブなプローブが 1 件目のアラートを解消したあと、2 件目を
  `"unhandled"` のまま報告します。`cleared` は `True` になりますが `blocked_note` はセットされたまま
  であり、呼び出しが何かを解消していたとしても、最終的な失敗理由にその注記が届くことを確認します。
- タップの着地レースのケース: `dismiss_from_tree_once` が、あるラウンドの 1 回目の試行では
  `"not_tappable"` を返し、後のラウンドで解消します。それでもアラートが解消されること、ループが
  `_GUARD_CALL_MAX_ROUNDS` の上限内に収まることを確認します。
- 恒常的に塞がれているケース: どのラウンドのタップも `"not_tappable"` を返し続けます。ループが
  `_GUARD_CALL_MAX_ROUNDS` で止まり、ステップが失敗し、失敗理由が `uncleared_prompt_note(label)` を
  通じてアラートを名指しすること、単なる要素の欠落として読めてしまわないことを確認します。
- 上限到達時にも落ち着かせが働くケース: 3 件のプロンプトが重なって、それぞれ解消され、最後の解消の
  直後に `_GUARD_CALL_MAX_ROUNDS` を使い切ります。`__call__` が戻る前に `settle_after_alert_dismiss`
  がすでに実行されていて、続く再試行がアニメーション中のツリーではなく、落ち着いたツリーを読むことを
  確認します。
- 既存の単一アラート・単一ラウンドの挙動を守る回帰テスト: ツリー内アラートが 1 件だけあり、1 回目の
  試行で解消するシナリオが、変更後も 1 ラウンドで解消することを確認します。この場合にループが余分な
  遅延を持ち込まないことを確かめます。

### ユニット 5 — ドキュメント

[`docs/architecture.md`](../../docs/architecture.md) の `systemAlertHandling` を扱う節（705〜787 行目
付近）と、その [`docs/ja/`](../../docs/ja/architecture.md) の対応箇所に、ステップ終了時と `expect` の
ガードが 1 回の呼び出しで複数件のアラートを解消できるようになった旨を、短く加えます。この記述は、
`wait` 中のガードについて同じ挙動をすでに説明している箇所と並べて置きます。

### 今回の範囲外

本提案のあとも、次の 2 つのケースは意図的に未解決のまま残ります。省略ではありません。どの
`systemAlertHandling` ルールも名指ししないアラートは、今日と同じく `"unhandled"` という結果でループを
終え、何もタップしません。
[BE-0402](../BE-0402-run-alert-guard-drop-vision-fallback/BE-0402-run-alert-guard-drop-vision-fallback-ja.md)
は、シナリオが宣言していないアラートに答えを推測していた AI ビジョンのフォールバックを、まさにその
「宣言のない画面へのタップを推測すること自体が危険である」という理由で廃止しました。本提案は、その
判断を蒸し返しません。同じく、ツリー内のアラートのボタンが `_GUARD_CALL_MAX_ROUNDS` のうちに一度も
タップできる状態にならない場合も、ステップは未解決のまま失敗します。この 2 つのケースについて本提案が
変えるのは診断だけです。失敗が、ただの要素欠落として読める代わりに、`blocked_note` が記録したアラート
を名指しするようになります。どちらかのケースを実際に解消できるようにする、つまり、どのルールも名指し
しないアラートに答えるか、恒常的に塞がれた画面でも粘り強くリトライするかは、別の、より大きな提案の
話であり、まず BE-0402 自身の判断を見直すところから始める必要があります。

## 検討した代替案

| 案 | 概要 | 採らなかった理由 |
|---|---|---|
| `_AlertGuardGate` の `_tree_signature` の比較対象を、候補となるボタン集合だけに絞る | *`wait` 中の*再試行が疑われる「タップは着地した」という誤判定に対応するため、ポーリング間で比較する要素を減らす | 最初に検討した案です。`test_dismiss_from_tree_does_not_retry_when_the_tap_moved_the_screen` に照らして確認したところ、採用を見送りました。このテストが検証している場面（シートが閉じて、同じラベルを持つアプリ自身のボタンが現れる場面）は、そのアプリ自身のボタンを誤って再タップしないために、広い比較範囲を必要とします。範囲を絞ると、疑われていた不具合は直る一方で、このテストが守っている安全性そのものを失います。加えて、対象とする呼び出し箇所そのものが違います。報告された失敗は、`wait` 中の経路ではなく、ステップ終了時の単発経路に由来していました |
| `wait` 中のシグネチャ判定に、猶予（ポーリング 2 回連続で差分が続いたときだけ「別の画面になった」と判定する）を足す | 同じ発想を `wait` 中の経路だけに絞って手直しする、より小さな案 | 上と同じく、対象とする呼び出し箇所が違います。報告された失敗は通常のアクションステップのあとに起きており、`_AlertGuardGate` はそこでは一度も動きません |
| `dismiss_from_tree_once` に、`_alert_guard_gate.py` の `_TREE_RETAP_DELAY` ・ `_TREE_DISMISS_MAX_TAPS` ・ `_tree_signature` をそのまま複製した、独自の完全なリトライ状態機械を持たせる | `wait` 中の経路が持つ状態機械を、単発の経路へそのまま複製する | その状態機械は、`wait` ステップの持ち時間いっぱいに広がる多数のポーリングにまたがってタップを比較するために存在します。単発の呼び出しには、そもそも呼び出しをまたいで持ち越すべき履歴がありません。単発の呼び出しのなかで完結する、ラウンド数で上限を切った短いリトライのほうが、余分な状態やそれ自身の周辺ケースを抱えずに同じ目的を果たせます |
| 回避策をドキュメント化する: 複数件が重なりやすい、あるいはアニメーションが遅いアラートに出会いそうなアクションの前に、シナリオの作者みずから `wait` ステップを置くよう案内する | コードは変更せず、作者が割り込みのタイミングを予測することに頼る | [BE-0406](../BE-0406-system-alert-declared-prompts/BE-0406-system-alert-declared-prompts-ja.md) が、関連する別の欠落について同じ回避策とその欠点を記録しています。作者に OS のタイミングを予測させることになり、そのタイミングが本当に予測できない場合には、そもそも書きようがありません |

## 進捗

> 作業を進めるたびに、この節を最新の状態に保ってください。チェックリストは「詳細設計」の作業分割
> （MECE）を映し、作業単位ごとに 1 つの箱を用意します。ログには変更内容と日付を古い順に記録し、
> 関連する PR へリンクします。

- [x] ユニット 1 — 画面が晴れるか上限に達するまで `AlertGuardConfig.__call__` をループさせる
- [x] ユニット 2 — ツリー内タップでも、`wait` 中のガードと同じ着地レースを取りこぼさないようにする
- [x] ユニット 3 — `__call__` の契約を、解消したアラートをすべて報告する形に変える
- [x] ユニット 4 — テスト
- [x] ユニット 5 — ドキュメント

ログ：

- [#1979](https://github.com/bajutsu-e2e/bajutsu/pull/1979) で 5 つのユニットをすべて実装しました。
  `AlertGuardConfig.__call__` は `_GUARD_CALL_MAX_ROUNDS`（3）まで内部でループし、解消した 
  `AlertEvent` を呼び出し側が渡すリストへ追記し、1 件以上片付けたかどうかを真偽値で返します。
  `dismiss_from_tree_once` には `NotTappable` という帰結、`exclude` 引数、そして 
  `(result, buttons, signature)` という返り値を追加し、着地レースに対する有界の再試行を実装しました。2 か所の
  呼び出し元（`loop/_step_runner.py`、`loop/_functions.py`）はどちらも新しい 
  `(driver, alerts, *, settle) -> bool` という契約に移行し、注記の追記条件を、呼び出しが何かを片付
  けたかどうかではなく注記そのものの有無だけに揃えました。`docs/architecture.md` とその日本語版には、
  この複数ラウンドの挙動を追記しました。この PR に対するライブレビューでは、この重複排除の仕組み自
  体にさらにいくつかの正しさの問題が見つかり、同じ PR で修正しました。`probe_native` には 6 つ目の
  状態 `already_dismissed` を追加し、すでに答えたアラートへの再タップは、実機に届いて重複したイベン
  トを破棄するのではなく、届く前に見送るようにしました。重複排除の判定キーは、照会が読んだ生のボタ
  ン一覧ではなく、一致した規則の `identifying_labels` にしました。ボタン一覧は画面全体を数え上げた
  ものなので、別のアラートが加わった瞬間に変わってしまうためです。`_resolve_alert_rule` は、単純な
  一致がすでに答えた形に戻ってしまった場合、まだ答えていない形の中を探し直すようにし、フェードの奥
  に控えるアラートをネイティブ側とツリー側の両方で見つけられるようにしました。また、すでに答えた形
  に包まれるより狭い形についても、同じアラートへの一致として扱います。1 つのプロンプトに対する規則
  同士の形が入れ子になることがあるためで、`savePassword` の 3 つの形はその一例です。ラウンド上限を
  使い切った時点で画面に残っているのが、この呼び出しがすでにタップしたアラートだけだった場合は、注
  記を黙って空にするのではなく `uncleared_prompt_note` で報告するようにしました。高速なテストスイー
  トは、設計とライブレビューの双方が挙げているスタック済みアラート・着地レース・恒久的に塞がれたプ
  ロンプト・ラウンド上限を使い切るケース・入れ子になった形・ツリー側の除外が残り続けるケースを網羅
  しています。

  この PR の各ラウンドを通じて、5 つのファイルの記録済みカバレッジ下限が動きました。各ラウンドの完了
  時に `make coverage-floors` で受け入れています。3 つは 0.01〜0.03 ポイントずつ下がりました。いずれ
  も、新たに追加した未検証のロジックではありません。既存の無関係な未カバー行が、リファクタリング後の
  わずかに小さくなったファイルの中で占める割合が増えたことに起因すると、`coverage.json` 自身の記録で
  確認しています。対象は `loop/_functions.py`(97.41 → 97.38)です。`loop/_step_runner.py`(98.29 →
  98.28、元の複数ラウンド化)も同様です。もう 1 つは `waits/_functions.py`(97.9 → 97.88)です。
  `_tree_signature` を共有モジュール `drivers/elements.py` へ公開関数 `tree_signature` として移した
  変更に伴うものです。残る 2 つは同じ期間でむしろ上がりました。`waits/_alert_guard_gate.py`(97.07 →
  97.36)と `types/_functions.py`(97.46 → 97.75)です。後のラウンドで共有ヘルパー
  `identified_alert_rules` と `subtract_labels` を切り出し、その回帰テストを加えました。前のラウンド
  では一時的に未検証のままだった分岐が、これですべてカバーされました。

## 参考

- [BE-0269](../BE-0269-ios-alert-guard-early-wait-intervention/BE-0269-ios-alert-guard-early-wait-intervention-ja.md)
  — `wait` 中のガード自身の介入を扱った項目です。iOS のパスワード保存プロンプトが `wait` を止めて
  しまう現象の、最初の記録でもあります。
- [BE-0315](../BE-0315-ios-native-system-alert-handling/BE-0315-ios-native-system-alert-handling-ja.md)
  — `probe_native` が読む、ネイティブな SpringBoard クエリを扱った項目です。本提案は、この決定論的
  な経路を置き換えるのではなく、その経路をループさせます。
- [BE-0399](../BE-0399-ios-system-alert-interruption-policy/BE-0399-ios-system-alert-interruption-policy-ja.md)
  — 各プロンプトがどこに出るか、SpringBoard 側かアプリ自身のプロセス側かを扱った項目です。この違いが
  あるからこそ、複数件が重なった場合には、1 回の呼び出しのなかで 2 つの異なる解消経路が必要になります。
- [BE-0402](../BE-0402-run-alert-guard-drop-vision-fallback/BE-0402-run-alert-guard-drop-vision-fallback-ja.md)
  — `blocked_note` を導入した項目です。本提案はこれを置き換えず、複数ラウンドにわたる呼び出しへ
  そのまま拡張します。
- [BE-0406](../BE-0406-system-alert-declared-prompts/BE-0406-system-alert-declared-prompts-ja.md)
  — 本提案のループがラウンドの合間に再利用している、解消後に画面を落ち着かせる処理を加えた項目です。
  別の呼び出し箇所について、本提案のユニット 3 が扱うのと同じ過少報告のおそれも指摘していました。
