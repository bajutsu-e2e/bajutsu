[English](BE-0031-data-driven-scenarios.md) · **日本語**

# BE-0031 — データ駆動シナリオ

<!-- BE-METADATA -->
| 提案 | [BE-0031](BE-0031-data-driven-scenarios-ja.md) |
| 提案者 | [@0x0c](https://github.com/0x0c) |
| 状態 | **実装済み** |
| 実装 PR | [#6](https://github.com/bajutsu-e2e/bajutsu/pull/6) |
| トラック | [可決済み](../../README-ja.md#可決済み) |
| トピック | 競合調査（MagicPod / Autify）由来の候補 |
| 由来 | MagicPod |
<!-- /BE-METADATA -->

## はじめに

`data`（inline）/ `dataFile`（CSV）で 1 つのシナリオを複数行にわたって反復します。`${row.*}` を各行で置換します（`expand_data`）。多言語テストや境界値テストに利用できます。

## 動機

同じフローを多くの入力に対して実行したい場面はよくあります。複数のクエリでの検索、境界値ごとのフォーム、ロケールごとの画面などです。手で書くと、わずかなリテラルだけが違うほぼ同一のシナリオがケースの数だけでき、フローが変わるたびにすべてをまとめて編集することになります。フローは一度だけ書き、入力の表を与えることで、シナリオを複製するのではなく行を増やすだけでカバレッジを広げられるようにしたいのです。

## 詳細設計

シナリオは入力を `data`（インラインの行マッピングのリスト）か `dataFile`（CSV パス）として持ちます。両者は排他です（`bajutsu/scenario/` の `Scenario` は両方の指定を拒否します）。run の前に `expand_data` が、そうしたシナリオを 1 行 1 シナリオに置き換え、`${row.<column>}` を共通の `${...}` 補間プリミティブ（`interp.interpolate`、キーは `row.<column>`）で置換します。CSV の `dataFile` は列名を並べたヘッダ行を持ち、`read_csv` で読み込まれます。どちらのフィールドも持たないシナリオはそのまま素通りします。

派生シナリオは `"<name> [row N: col=val, …]"` に改名され、レポートや JUnit のテストケースが区別できるようになります。また元の preconditions を保つため（`erase` のデフォルトも含む）、各行ともアプリは fresh に再インストールされ、それぞれが独自のクリーンな環境で実行されます。補間は型を保ちます。ちょうど 1 つのトークンだけの文字列（`"${row.id}"`）は束縛された生の値を取り、より大きな文字列に埋め込まれたトークンはテキストとして差し込まれます。展開は完全にロード時に行われ `data`/`dataFile` フィールドを取り除くため、決定的なランナーは常にリテラルが埋まったプレーンなシナリオだけを見ます。決定性と行ごとの分離の両方が保たれます。

## 検討した代替案

**1 つのシナリオ内で行をランタイムループする。** 不採用。全行で 1 台のデバイスと 1 本の証跡ストリームを共有することになり、3 行目の失敗をきれいに分離できず、各ケースを個別の合否としてレポートに示せません。1 行 1 シナリオに展開すれば、各ケースが独自の fresh なデバイス、証跡、JUnit テストケースを持ちます。

**外部のテストマトリクスハーネス（ツールの外からパラメータ化する）。** 不採用。入力の表をシナリオファイルの外のラッパースクリプトに押し出し、「シナリオ YAML が共有のハブ」という前提と、入力をステップと並べてレビューできる利点を壊します。`data`/`dataFile` をシナリオ内に置けば、ケースはそれが動かすフローとともにバージョン管理でき、レビューできます。

## 参考

`bajutsu/scenario/`（`expand_data`）、[scenarios.md](../../../docs/ja/scenarios.md)
